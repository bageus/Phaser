{
  "name": "ursass-tube",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:tunnel-textures": "node scripts/generate-tunnel-textures.mjs",
    "check": "node scripts/check-syntax.mjs && npm run check:no-window-assign",
    "check:no-window-assign": "node scripts/check-no-new-window-assign.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
